import React, { ReactElement } from 'react';

export const HomePage = (): ReactElement => {
    return (
        <main>
            <h2>HomePage</h2>
        </main>
    );
};

export default HomePage;
